/** Shared body/description text style for cards (descriptions, notes, captions). */
export const bodyTextClass = 'text-xs leading-snug text-slate-600';

/** Outer section wrapper: rounded card with consistent padding. */
export const sectionClass = 'rounded-xl bg-white shadow-sm border border-slate-200 p-4 sm:p-5 flex flex-col space-y-3';

/** Section heading (h2). */
export const sectionTitleClass = 'text-base font-semibold leading-tight text-slate-900 flex items-center gap-1.5';

/** Horizontal divider inside a section. */
export const sectionDividerClass = 'border-b border-slate-100';

/** Standard inner card used across Combat, Limited Uses, Traits, Reactions, Inventory. */
export const innerCardClass = 'rounded-lg border border-slate-200 bg-slate-50/50 p-3';

/** Sub-section header (h3) inside a section. */
export const subSectionHeaderClass = 'text-xs font-semibold text-slate-600';

/** Inline badge/chip for type labels. */
export const badgeClass = 'text-[0.65rem] font-medium uppercase tracking-wide px-1.5 py-0.5 rounded';
